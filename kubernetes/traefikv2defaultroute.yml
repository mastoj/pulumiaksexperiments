apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fallback
annotations:
  traefik.ingress.kubernetes.io/router.priority: "1" 

spec:
  defaultBackend:
    service:
      name: traefikv1
      port:
        number: 80